# <Spec Title> Модель данных

## Scope

- Связанные `AC-*`: `AC-001`
- Связанные `DEC-*`: `DEC-001`
- Явно укажите, если для этой фичи значимого изменения data model не требуется

## Сущности

### DM-001 Сущность 1

- Назначение:
- Источник истины:
- Инварианты:
- Связанные `AC-*`:
- Связанные `DEC-*`:
- Поля:
  - `field_name` - тип или shape, required или optional, смысл, default или validation rule если это важно
- Жизненный цикл:
  - когда и каким действием создается
  - когда и каким действием обновляется
  - когда удаляется, истекает или архивируется
- Замечания по консистентности:
  - какое stale, duplicated, out-of-order или partially-written состояние недопустимо

### DM-002 Сущность 2

- Назначение:
- Источник истины:
- Инварианты:
- Связанные `AC-*`:
- Связанные `DEC-*`:
- Поля:
  - `field_name` - тип или shape, required или optional, смысл, default или validation rule если это важно
- Жизненный цикл:
  - когда и каким действием создается
  - когда и каким действием обновляется
  - когда удаляется, истекает или архивируется
- Замечания по консистентности:
  - какое stale, duplicated, out-of-order или partially-written состояние недопустимо

## Связи

- `DM-001 -> DM-002`: связь, ownership или cardinality
- Явно укажите, если значимых межсущностных связей нет

## Производные правила

- Вычисляемое значение, правило нормализации или правило перехода состояния
- Явно укажите, если производные правила не нужны

## Переходы состояний

- Trigger или event -> предыдущее состояние -> следующее состояние
- Validation или guard, который не дает выполнить некорректный переход
- Явно укажите, если жизненный цикл достаточно прост и отдельный список переходов не нужен

## Вне scope

- Сущность, поле или состояние, которые намеренно не моделируются здесь
